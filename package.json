{
  "name": "dvault",
  "version": "1.0.0",
  "description": "Dvault app",
  "scripts": {
    "build": "echo Build complete",
    "cap": "cap",
    "sync": "npx --yes cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
